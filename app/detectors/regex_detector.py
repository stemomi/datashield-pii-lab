"""Regex-based detector scaffolding."""

from __future__ import annotations


def detect_with_regex(_: str) -> list[dict[str, str]]:
    """Detect candidate entities using regex rules."""
    raise NotImplementedError("Regex detection will be added in phases 4 to 6.")
