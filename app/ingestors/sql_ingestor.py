"""SQL ingestion placeholder for a later phase."""

from __future__ import annotations

from pathlib import Path


def load_sql(_: Path) -> str:
    """Load SQL export content from disk."""
    raise NotImplementedError("SQL ingestion is planned after the MVP.")
